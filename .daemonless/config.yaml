icon: ':simple-onlyoffice:'

cit:
  mode: port
  port: 80
  wait: 120
  screenshot: /example/

build:
  variants:
    - tag: pkg
      containerfile: Containerfile.pkg
      default: true
    - tag: pkg-latest
      containerfile: Containerfile.pkg
      args:
        BASE_VERSION: "15-latest"
